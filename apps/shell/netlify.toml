[build]
  command = "npm ci && npx nx build destinations --configuration=production"
  publish = "dist/apps/destinations"

[build.environment]
  NODE_ENV = "production"
  NX_IGNORE_UNSUPPORTED_TS_SETUP = "true"

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"